<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:gravity="center"
    android:orientation="vertical"
    android:id="@+id/primary_layout"
    >
    <androidx.appcompat.widget.AppCompatTextView android:id="@+id/moves"
        android:text="Tic Tak Toe"
        android:layout_width="wrap_content" android:layout_height="wrap_content"
        android:layout_gravity="center" android:textSize="35sp" android:textStyle="bold"
        android:layout_marginBottom="30dp" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" android:text="Players" android:textStyle="bold" />
    <Spinner android:layout_height="wrap_content" android:layout_width="wrap_content"
        android:id="@+id/playersSpinner"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:spinnerMode="dropdown"
        android:paddingHorizontal="50dp"
        android:paddingVertical="5dp"
        android:popupBackground="@color/white"
       />
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content" android:text="Choose token" android:textStyle="bold" />
    <Spinner android:layout_height="wrap_content" android:layout_width="wrap_content"
        android:id="@+id/tokensSpinner"
        android:layout_marginTop="16dp"
        android:layout_marginBottom="16dp"
        android:spinnerMode="dropdown"
        android:paddingHorizontal="50dp"
        android:paddingVertical="5dp"
        android:popupBackground="@color/white"
        />
<androidx.appcompat.widget.AppCompatButton
    android:id="@+id/start"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:text="Start"
    android:background="@drawable/btnstyle"
    android:textColor="@color/white"
    android:drawableLeft="@drawable/baseline_start_24"
    android:paddingLeft="40dp"
    android:paddingRight="40dp"
    android:drawablePadding="8dp"
    android:tint="@color/white"
    />
</LinearLayout>